apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: userauth-db-claim0
  name: userauth-db-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
