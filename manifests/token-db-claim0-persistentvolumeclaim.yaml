apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: token-db-claim0
  name: token-db-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
