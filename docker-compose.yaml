services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Docker/development/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - "./frontend/app/:/app/app/"
  postgres:
    image: postgres:17.5-alpine3.21
    container_name: postgres_db
    restart: always
    env_file:
      - .env
    volumes:
      - ./db-postgres/pgdata:/var/lib/postgresql/data # Persistencia de datos
      - ./db-postgres/initdb:/docker-entrypoint-initdb.d # Comando de inicialización
    ports:
      - "5432:5432"
  mongo:
    image: mongo:6
    container_name: mongo_db
    restart: always
    env_file:
      - .env
    volumes:
      - ./db-mongo/data:/data/db  # Persistencia de datos
      - ./db-mongo/initdb:/docker-entrypoint-initdb.d:ro # Comando de inicialización
    ports:
      - "27018:27017"    # mapea 27018 en el host al 27017 del contenedor
