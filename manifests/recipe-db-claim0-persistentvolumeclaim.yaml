apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: recipe-db-claim0
  name: recipe-db-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
